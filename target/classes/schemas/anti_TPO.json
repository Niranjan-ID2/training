{
  "type": "object",
  "properties": {
    "reportDate": { "type": "string", "format": "date" },
    "labName": { "type": "string" },
    "patientName": { "type": "string" },
    "patientAge": { "type": "integer", "minimum": 0 },
    "patientSex": { "type": "string" },
    "values": {
      "type": "object",
      "properties": {
        "value": { "type": "number" },
        "unit": { "type": "string", "enum": ["IU/mL"] }
        // Optional: Add reference_range if it's standard for this test
      },
      "required": ["value", "unit"]
    }
  },
  "required": ["reportDate", "labName", "patientName", "patientAge", "patientSex", "values"]
}
